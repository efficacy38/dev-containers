version: '3.6'
services:
  base-ct:
    image: base-ct:latest
    build: 
      context: .
      dockerfile: ./base-ct.df
  dev-ct:
    image: dev-ct:latest
    build: 
      context: .
      dockerfile: ./dev-ct.df
    restart: unless-stopped
